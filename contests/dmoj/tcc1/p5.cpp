#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;
ll m, k;

int main()
{
    cin.tie(0);
    cin.sync_with_stdio(0);
    cin >> m >> k;
    printf("%lld\n", m / 2)

        return 0;
}
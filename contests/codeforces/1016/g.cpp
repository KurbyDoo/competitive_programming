#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;
ll t, n, k;
ll nums[200010];
vector<int> trie;
struct Node {
    int val;
    int left;
    int right;
}
int main(){
    cin.tie(0);
    cin.sync_with_stdio(0);
    
    return 0;
}
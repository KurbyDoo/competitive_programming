#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef pair<int, int> pii;
int t;

void solve() {
    int n;
    cin >> n;
    bool dp[2][n];
    string s;
    cin >> s;
    if (s[0] == "'1") dp[]
    for (int i = 1; i < n; i++)
}

int main(){
    cin.tie(0);
    cin.sync_with_stdio(0);
    cin >> t;
    for (int i = 0; i < t; i++) {
        solve();
    }

        return 0;
}